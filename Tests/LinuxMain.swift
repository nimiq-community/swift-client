import XCTest

import NimiqClientTests

fatalError("Running tests like this is unsupported. Run the tests again by using `swift test --enable-test-discovery`")

var tests = [XCTestCaseEntry]()
XCTMain(tests)
